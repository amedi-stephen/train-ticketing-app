import { SEARCH_TRAIN } from "../actions/types";

const initialState = {
    trains: []
}

export default function(state=initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}